import { slider } from "./customSlider";
import { generateArrows } from "./generateArrows";
import { generateDots } from "./generateDots";
import { fadePlugin, infinityScroll } from "./plugins";
export const customSlider = {
  generateArrows,
  generateDots,
  slider,
  fadePlugin,
  infinityScroll
};
